use super::*;

#[cfg(test)]
pub mod tests {
    use super::*;

    #[test]
    fn void_svg_out_test() {
        let svg = svg_out(2, 2);
        
        let mut file = File::open("../../
        
        assert_eq!(svg, "4");
    }
}
